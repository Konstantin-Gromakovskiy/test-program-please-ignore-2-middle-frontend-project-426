front-build:
	cd front && npm run build
back-build:
	cd back && npm run build
front-dev:
	cd front && npm run dev
back-dev:
	cd back && npm run dev
front-install:
	cd front && npm install
back-install:
	cd back && npm install
